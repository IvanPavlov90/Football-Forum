@using NLog;
@{
    Logger logger = LogManager.GetCurrentClassLogger();
    try
    {
        FormsAuthentication.SignOut();
        Response.Redirect("~/Pages/LogIn", false);
    }
    catch (Exception ex)
    {
        logger.Error(ex, $"{ex.Message} - came from signout page.");
        Response.Redirect("~/Pages/ErrorPage");
    }
}
